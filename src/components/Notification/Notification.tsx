import css from './Notification.module.css';
import React from 'react';

const Notification: React.FC = () => {
  return <p className={css.message}>No feedback ye</p>;
};

export default Notification;
